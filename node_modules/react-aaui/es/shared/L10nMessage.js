import React from 'react';
import { string, object } from 'prop-types';
import isNil from 'lodash/isNil';

import injectL10n from './injectL10n';
import { aauiL10nShape } from './types';

function L10nMessage(_ref) {
  var id = _ref.id,
      values = _ref.values,
      l10n = _ref.l10n,
      htmlId = _ref.htmlId,
      defaultMessage = _ref.defaultMessage;

  var props = {};
  if (htmlId) {
    props.id = htmlId;
  }
  return React.createElement(
    'span',
    props,
    l10n.formatMessage(id, values, isNil(defaultMessage) ? id : defaultMessage)
  );
}

L10nMessage.propTypes = {
  id: string,
  values: object, // eslint-disable-line
  l10n: aauiL10nShape,
  messageId: string,
  defaultMessage: string
};

export default injectL10n()(L10nMessage);